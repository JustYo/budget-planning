apiVersion: v1
kind: Service
metadata:
  name: eb-importer
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "terra-budget.labels" . | nindent 4 }}
spec:
  selector:
    app: eb-importer
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
